import 'package:flutter/material.dart';

class ConsultationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Consultations or Games'),
      ),
      body: Center(
        child: Text('Consultations or Games Content Here'),
      ),
    );
  }
}
