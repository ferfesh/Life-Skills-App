import 'package:flutter/material.dart';

class SessionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Session'),
      ),
      body: Center(
        child: Text('Session Content Here'),
      ),
    );
  }
}
